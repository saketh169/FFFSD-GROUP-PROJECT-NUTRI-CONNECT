<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ADMIN Home Page</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        /* Custom Styles */
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #eef2f3, #d9e3f0);
            color: #333;
        }

        .welcome-section {
            padding: 80px 0;
            background: linear-gradient(rgba(255, 255, 255, 0.95), rgba(255, 255, 255, 0.95)), url('https://via.placeholder.com/1500');
            border: 5px solid #007bff;
            border-radius: 20px;
            max-width: 95%;
            margin: 50px auto;
            animation: fadeIn 1.5s ease-in-out;
        }

        .admin-img {
            transition: transform 0.3s ease-in-out;
            border-radius: 15px;
            transition: 0.3s;
        }
        .admin-img:hover {
            transform: scale(1.02);
        }

        .btn-primary {
            border-radius: 30px;
            transition: all 0.3s ease-in-out;
            color: #fff;
            font-weight: 600;
            padding: 10px 25px;
        }

        .btn-primary:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 10px rgba(0, 123, 255, 0.5);
        }

        .btn-secondary {
            border-radius: 30px;
            transition: all 0.3s ease-in-out;
            color: #fff;
            font-weight: 600;
            padding: 10px 25px;
            background-color: #6c757d; /* Gray color for the Queries button */
            border: none;
        }

        .btn-secondary:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 10px rgba(108, 117, 125, 0.5);
        }

        h1.display-4 {
            font-weight: 700;
            color: #007bff;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
            animation: slideInLeft 1s ease-in-out;
        }

        p.lead {
            font-size: 1.25rem;
            color: #555;
            animation: slideInRight 1s ease-in-out;
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @media(max-width:992px){
            .admin-img{
                margin-top: 50px;
            }
        }
    </style>
</head>
<body>

    <!-- Admin Welcome Section -->
    <section class="welcome-section admin-section">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <h1 class="display-4 fw-bold text-primary mb-4">Welcome, Admin Team</h1>
                    <p class="lead mb-4">
                        Manage Nutri Connect seamlessly with powerful tools to oversee users, dietitians, and operations.
                         Keep the platform secure, monitor activities, and ensure a smooth experience for all.
                    </p>
                    <div class="d-flex gap-3">
                        <a href="/admin_dash" class="btn btn-primary btn-lg">Go to Dashboard</a>
                        <a href="/queries" class="btn btn-secondary btn-lg">Queries</a>
                    </div>
                </div>
                <div class="col-lg-6">
                    <img src="https://img.freepik.com/free-vector/couple-professionals-analyzing-graphs_74855-4393.jpg?t=st=1741698166~exp=1741701766~hmac=7b63d82c02a0361ce7dfd83a0b0b2e4f36a0091dafa3b549232d82b8470142c0&w=1480" alt="Admin Dashboard" class="img-fluid rounded  admin-img" />
                </div>
            </div>
        </div>
    </section>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>