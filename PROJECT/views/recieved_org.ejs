<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Organization Document Verification</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        /* General Styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #e8f5e9; /* Light green background */
            margin: 0;
            padding: 20px;
        }
        h1 {
            color: #1b5e20; /* Dark green for the header */
            text-align: center;
            margin-bottom: 30px;
            font-size: 2.5em;
            animation: fadeIn 1s ease-in-out;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            animation: slideIn 0.5s ease-in-out;
        }
        .document-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px;
            border-bottom: 1px solid #ddd;
            transition: all 0.3s ease;
        }
        .document-row:last-child {
            border-bottom: none;
        }
        .document-row:hover {
            background-color: #f1f8e9; /* Light green hover effect */
            transform: translateX(10px); /* Slight movement on hover */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .document-row span {
            font-size: 1.1em;
            transition: color 0.3s ease;
        }
        .document-row:hover span {
            color: #1b5e20; /* Darker green on hover */
        }
        .status {
            font-weight: bold;
            transition: color 0.3s ease;
        }
        .status.verified {
            color: #4caf50; /* Green for verified */
        }
        .status.pending {
            color: #ff9800; /* Orange for pending */
        }
        .status.rejected {
            color: #e53935; /* Red for rejected */
        }
        .status.received {
            color: #1b5e20; /* Dark green for received */
        }
        .final-report {
            margin-top: 20px;
            padding: 15px;
            background-color: #f1f8e9; /* Very light green for final report section */
            border-radius: 10px;
            text-align: center;
            animation: fadeIn 1s ease-in-out;
        }
        .final-report h3 {
            margin-top: 0;
            color: #1b5e20; /* Dark green for header */
            font-size: 1.5em;
        }
        .final-report .status {
            font-size: 1.2em;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            font-size: 14px;
            color: #888;
            animation: fadeIn 1s ease-in-out;
        }
        .footer a {
            color: #2e7d32; /* Dark green for footer links */
            text-decoration: none;
            transition: color 0.3s ease;
        }
        .footer a:hover {
            color: #1b5e20; /* Darker green on hover */
            text-decoration: underline;
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        @keyframes slideIn {
            from { transform: translateY(-20px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        /* Hover Effects for Icons */
        .document-row i {
            transition: transform 0.3s ease, color 0.3s ease;
        }
        .document-row:hover i {
            transform: scale(1.2); /* Enlarge icons on hover */
            color: #1b5e20; /* Darker green on hover */
        }

        /* Final Report Hover Effect */
        .final-report:hover {
            background-color: #e6f5e9; /* Light green hover effect */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        #cross {
            position: fixed;
            top: 5px;
            left: 10px;
            line-height: 20px;
            font-size: 40px;
            border-radius: 100%;
        }
        </style>
    
        
    </head>
    <body>
    
    <!-- Back Button -->
    <div id="cross" class="btn btn-outline-success" onclick="window.history.back();">
        <i class="fa-solid fa-xmark"></i>
    </div>

    <h1> Document Verification</h1>
    <div class="container">
        <!-- Organization Name -->
        <h2><i class="fas fa-building"></i> GreenTech Solutions</h2>

        <!-- Document Status -->
        <div class="document-row">
            <span><i class="fas fa-image"></i> Organization Logo</span>
            <span class="status verified"><i class="fas fa-check-circle"></i> Verified</span>
        </div>
        <div class="document-row">
            <span><i class="fas fa-file-pdf"></i> Organization Brochure</span>
            <span class="status pending"><i class="fas fa-hourglass-half"></i> Pending</span>
        </div>
        <div class="document-row">
            <span><i class="fas fa-file-contract"></i> Legal Document</span>
            <span class="status verified"><i class="fas fa-check-circle"></i> Verified</span>
        </div>
        <div class="document-row">
            <span><i class="fas fa-file-invoice-dollar"></i> Tax Document</span>
            <span class="status rejected"><i class="fas fa-times-circle"></i> Rejected</span>
        </div>
        <div class="document-row">
            <span><i class="fas fa-map-marker-alt"></i> Proof of Address</span>
            <span class="status pending"><i class="fas fa-hourglass-half"></i> Pending</span>
        </div>
        <div class="document-row">
            <span><i class="fas fa-id-card"></i> Business License</span>
            <span class="status verified"><i class="fas fa-check-circle"></i> Verified</span>
        </div>

        <!-- Final Report Status -->
        <div class="final-report">
            <h3><i class="fas fa-file-contract"></i> Final Report Status</h3>
            <span class="status received"><i class="fas fa-check-circle"></i> Received</span>
        </div>
    </div>

    <!-- Footer -->
    <div class="footer">
        <p>If you have any questions, please contact our support team at <a href="mailto:support@orgverify.com"><i class="fas fa-envelope"></i> support@orgverify.com</a>.</p>
    </div>
</body>
</html>